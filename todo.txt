(A) HELP_ARTICLES aus help_dialog.py (549 der 779 Zeilen) nach locales/{de,en,sv}.json auslagern und über tr() laden. Größte verbleibende i18n-Lücke, blockiert M4. +Refactoring @code
(A) refresh_ui_labels() in den übrigen 27 Dialogklassen ergänzen (bisher nur ProfileSettingsDialog). Ohne das aktualisiert ein Sprachwechsel zur Laufzeit die Dialoge nicht. Voraussetzung für M5. +Refactoring @code
(B) Hartcodierte deutsche Statusmeldungen auf tr() umstellen: ai_assistant_dialog, email_calendar_dialog, email_import_dialog, export_dialog, p2p_diff_dialog. Fallen bei einem Grep nach fehlendem tr( nicht auf, weil die Dateien sonst tr-lastig sind. +Refactoring @code
(B) profile_settings_dialog.py (1184 Zeilen) entlang der 6 Tabs in Mixins aufteilen. Vorbild ist der bereits ausgelagerte AiSettingsTabMixin. setup_user_tab allein hat 211 Zeilen, save_settings validiert alle 6 Tabs in einer Methode. +Refactoring @code
(B) Retention für die täglichen Backups: perform_daily_backup legt pro Tag eine Vollkopie von cases.json an, es gibt keinerlei Aufräumlogik. Nach zwei Jahren Nutzung liegen dort über 700 Dateien. +Bug @code
(B) Suchindex für die Volltextsuche: SearchService.matches_query baut pro Tastendruck für jeden Fall einen Suchstring aus ~10 Feldern inklusive aller Timeline-Notizen neu zusammen. Gecachten searchable_text pro Fall vorhalten und bei Änderung invalidieren. +Refactoring @search
(C) Cockpit: on_click_save ruft form_widget.load_schema() auf und baut damit das komplette Formular neu auf, nur um die Rahmenfarben der Pflichtfelder zu setzen. Stattdessen gezielt border_color der betroffenen Felder konfigurieren. +Refactoring @gui
(C) Board: toggle_column_collapse ruft create_board() und danach refresh_board() auf und reißt dadurch alle vier Spalten samt Karten zweimal ein. Nur den betroffenen Spalten-Frame austauschen. +Refactoring @gui
(C) Unsaved-Guard auf die restlichen Formulardialoge ausweiten (BaseDialog.enable_unsaved_guard). Aktiv in NewCaseDialog und EmailDraftDialog. Dialoge mit Liste plus Formular brauchen zusätzlich mark_clean() beim Auswahlwechsel, sonst fragen sie zu oft nach. +NewFeature @gui
(C) Tests für die neuen Dialog-Pfade ergänzen: Escape schließt, Enter löst die Primäraktion aus, Löschbestätigung, Rückgabe des Input-Grabs an den darunterliegenden Dialog. Die Suite erzeugt bisher keine Tastatur-Events. +Validation @code
(D) app.py weiter entflechten: ReminderScheduler, WindowChrome und TrayIntegration als eigene Mixins, Vorbild ist der bestehende DialogLaunchersMixin. +Refactoring @code
